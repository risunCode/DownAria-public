/**
 * API Response Types
 */

import type { 
    MediaData,
    DownloadResponse
} from '../types';

// Re-export types from ../types for convenience
export type { MediaData, DownloadResponse } from '../types';
